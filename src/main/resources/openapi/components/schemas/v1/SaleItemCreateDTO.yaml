type: object
properties:
  productId:
    type: integer
    format: int64
  quantity:
    type: integer
    format: int32
  unitPrice:
    type: number
    format: double
required:
  - productId
  - quantity
  - unitPrice